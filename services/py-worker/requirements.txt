# Deliberately pinned to outdated, vulnerable releases.
# Managed by Renovate's `pip_requirements` manager and Dependabot's `pip` ecosystem.
certifi==2022.12.7
flask==2.0.3
jinja2==3.0.3
requests==2.33.0
urllib3==1.26.15
werkzeug==2.0.3
